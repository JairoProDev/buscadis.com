# Sprint 7 — Chrome + composer Implementation Plan

> **For agentic workers:** Checkboxes track progress.

**Goal:** Permanent mobile chrome ≤112px (header 56 + nav 56); category accents; composer keyboard; filter CSS collapse.

**Done:** Header/Navbar heights + CSS vars; nav pinned; category 3px bar; ComposerModeToggle arrows; FilterSectionCard without Framer.
