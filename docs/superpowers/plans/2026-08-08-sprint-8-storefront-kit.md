# Sprint 8 — Storefront kit Implementation Plan

> **For agentic workers:** Checkboxes track progress.

**Goal:** `@buscadis/storefront-kit` with closed 5-var tenant contract, `derivarTemaTenant` (OKLCH + AA), nocturno preset, stop inventing marketplace `--bg-*` in JS.

**Done:** package + bridge CSS `[data-tenant]`; `npm run smoke:storefront`.
