# Perfil Vivo — P14 lite analytics

**Date:** 2026-08-08  
**Source:** `09` perfil_visto

## Shipped

- `PerfilVivoAnalytics` on `/v/[slug]` (skip demo)
- Reuses `trackProfileView` + `profile_view` + `publish.step_view` payload `{ step: 'perfil_visto', origen, superficie }`

## Next (full P14)

Panel dueño, informe WA mensual, taxonomía completa de `09`.
