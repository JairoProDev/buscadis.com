/**
 * Shell layout for public business profile pages.
 * Orchestrates hero, action bar, tabs, and modular tab content.
 * State and catalog rendering live in BusinessPublicView until full catalog extraction.
 */
export { default } from '../BusinessPublicView';
